<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dialog"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <Spinner
        android:id="@+id/spinner_books_dialog"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:entries="@array/classes_lables"
        android:layout_marginLeft="4dp"
         />
<!-- android:layout_marginLeft="4dp" 调整他保证左侧与DialogAlert对齐 -->
    <Spinner
        android:id="@+id/spinner_classes_dialog"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="4dp"
         />
    <!-- android:gravity="left" 文字靠左 -->

   <!-- 音乐播放器布局 -->

   <!--   <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:gravity="center_vertical"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/music_play"
            android:layout_width="42dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:src="@drawable/lock_play" />

        <CheckBox
            android:id="@+id/checkBox_spread"
            android:text="展开"
            android:layout_width="65dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
             />

        <CheckBox
            android:id="@+id/checkBox_random"
            android:text="随机"
            android:layout_width="56dp"
            android:layout_weight="1"
            android:layout_height="match_parent" />
    </LinearLayout>
    -->





</LinearLayout>